- route:
    id: order-processing-route
    from:
      id: from-orders
      uri: kamelet:spring-rabbitmq-source
      parameters:
        host: "{{rabbitmq.host}}"
        port: "{{rabbitmq.port}}"
        username: "{{rabbitmq.username}}"
        password: "{{rabbitmq.password}}"
        exchangeName: amq.direct
        queues: orders.requests
        routingKey: orders.requests
        autoDeclare: false
      steps:
        - doTry:
            id: doTry-orders
            doCatch:
              - id: catch-all
                exception: java.lang.Exception
                steps:
                  - log:
                      id: log-5eca
                      message: "Error processing order: ${exception.message}"
                  - to:
                      id: send-to-dlq
                      uri: kamelet:spring-rabbitmq-sink
                      parameters:
                        host: "{{rabbitmq.host}}"
                        port: "{{rabbitmq.port}}"
                        username: "{{rabbitmq.username}}"
                        password: "{{rabbitmq.password}}"
                        exchangeName: amq.direct
                        queues: orders.dlq
                        routingKey: orders.dlq
                        autoDeclareProducer: false
            steps:
              - setHeader:
                  id: set-correlation
                  name: CamelCorrelationId
                  expression:
                    simple:
                      id: simple-7e1f
                      expression: ${body.correlationId}
              - setBody:
                  id: transform-order
                  expression:
                    simple:
                      id: simple-c2b9
                      expression: |
                        {
                          "customer_ID": "${body.customer_ID}",
                          "orderDate": "${date:now:yyyy-MM-dd}",
                          "orderAmount": ${body.totalAmount},
                          "currency_code": "${body.currency_code}",
                          "items": ${body.items}
                        }
              - setHeader:
                  id: set-content-type
                  name: Content-Type
                  expression:
                    constant:
                      id: constant-5d53
                      expression: application/json
              - to:
                  id: call-erp
                  uri: http
                  parameters:
                    httpMethod: POST
                    httpUri: http://erp:4004/odata/v4/simple-erp/Orders
              - log:
                  id: log-erp-response
                  message: "ERP response received: ${body}"
              - setBody:
                  id: build-response
                  expression:
                    simple:
                      id: simple-7b91
                      expression: |
                        {
                          "correlationId": "${header.CamelCorrelationId}",
                          "erpResponse": ${body}
                        }
              - to:
                  id: send-response
                  uri: kamelet:spring-rabbitmq-sink
                  parameters:
                    host: "{{rabbitmq.host}}"
                    port: "{{rabbitmq.port}}"
                    username: "{{rabbitmq.username}}"
                    password: "{{rabbitmq.password}}"
                    exchangeName: amq.direct
                    queues: orders.responses
                    routingKey: orders.responses
                    autoDeclareProducer: false
